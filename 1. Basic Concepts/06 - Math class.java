// MATH CLASS

// Generating a random number between 0 to 100

int number = (int) Math.round(Math.random() * 100);		// one way
int number = (int) (Math.random() * 100));				// second way
System.out.println(number);